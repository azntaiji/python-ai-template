# Session 0 — Kickoff Prompt

Use this once, in the very first agent session. Replace EVERY angle-bracket
value (including the ones inside the `Names:` line), then paste everything
below the line as the prompt. This session
only populates `PLAN.md` — implementation starts in the next session with
`PROMPT_RESUME.md`.

---

You are working in a Python project repository. Read `AGENTS.md`, then do
exactly one job and stop:

Fill in `PLAN.md` using the context below. Replace the `—` placeholders in
the Goal, Scope, and Done Criteria sections, and add this line to the
Architecture section exactly as written (Task 3 of the setup track reads it):

    - Names: project=<project-name-with-hyphens>, package=<package_name_with_underscores> (decided <today's date>)

Do not change any other file. Do not start any Task.

Project context:

- Goal: <one or two sentences: what this project delivers>
- In scope: <bullet list>
- Out of scope: <bullet list>

When `PLAN.md` is updated, report what you changed and stop.
