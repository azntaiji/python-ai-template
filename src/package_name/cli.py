"""Command-line interface for the <package_name> package."""

from <package_name>.logging_config import setup_logging

def main() -> None:
    """Run the CLI entrypoint."""
    setup_logging()
    print("<package_name> CLI — not yet implemented")

if __name__ == "__main__":
    main()